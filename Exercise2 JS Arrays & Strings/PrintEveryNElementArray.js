function solve(arr,num) {
    const newArr = [];
    for (let index = 0; index < arr.length; index+=num) {
      newArr.push(arr[index]);
       
       
    }return newArr;
}


solve(['dsa',
'asd', 
'test', 
'tset'], 
2
);










